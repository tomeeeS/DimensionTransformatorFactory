package com.company;

public class DarkMatter extends Product {
    private float depth;

    public DarkMatter( String name, float depth ) {
        super( name );
        this.depth = depth;
    }
}
